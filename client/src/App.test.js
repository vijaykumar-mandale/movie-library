import { render, screen } from '@testing-library/react';
import App from './App';

describe("App", () => {
  test("Renders App", () => {
    // Arrange
    //const {appRender} = render(<App />);

    //screen.debug();
    //logRoles(appRender);
    // Act
    // ... nothing

    // Assert
    // const myAppComponent = screen.getByTestId('myapp', {exact: false})
    //  expect(myAppComponent).toBeInTheDocument();
  });
});
